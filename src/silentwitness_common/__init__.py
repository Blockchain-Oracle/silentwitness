"""SilentWitness shared types — Pydantic models reused across MCP + agent.

Single source of truth for cross-package contracts (Observation, Interpretation,
ResponseEnvelope, AuditEntry, etc.). See ``docs/architecture.md`` §5.2 for the
canonical envelope shape.
"""
