# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/
htmlcov/
.coverage
.coverage.*
coverage.xml
*.cover

# Project-specific
# Per-case audit/evidence/report — never committed.
cases/
# Local evidence corpora copied to VPS/workstations for demo runs.
/evidence/
# HMAC verification ledgers — never committed.
var/lib/silentwitness/
# Binary forensic images — manifests are committed, binaries are not.
harness/datasets/*.E01
harness/datasets/*.E0[2-9]
harness/datasets/*.pcap
harness/datasets/*.zip
harness/datasets/*.tar
harness/datasets/*.tgz
harness/datasets/*.dmg
harness/datasets/*.iso
# Benchmark output — committed per-release only.
harness/results/
# Synthetic adversary case (Epic 15 CUT — never used).
harness/case-trapdoor/output/

# VPS/demo run logs.
vps-verification-*/
log2timeline*.log.gz
pinfo*.log.gz
psort*.log.gz
rocba-live*.log

# SBOM + license artifacts (generated)
sbom.cdx.json
licenses.json

# Detected-secrets baseline file (committed; per-developer scan output is not)
.secrets.scan.json

# Editor / OS
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Env vars / secrets
.env
.env.local
.env.*.local
!.env.example

# Tool caches that don't belong in version control
.cache/
node_modules/
package-lock.json
yarn.lock
# pnpm-lock.yaml IS committed (supply-chain pin; CI uses --frozen-lockfile)

# Node / Next.js / Fumadocs site under apps/site/
apps/**/.next/
apps/**/.source/
apps/**/.vercel/
apps/**/*.tsbuildinfo
apps/**/public/pagefind/
apps/**/public/diagrams/
.pnpm-store/

# Sub-agent research scratch
/tmp/
.claude/*.lock
*,cover
docs/EVIDENCE_INVENTORY.md
tests/fixtures/.harness-*.log

# Browser-driver capture artifacts (Playwright MCP)
.playwright-mcp/

# AI-orchestration spec residue — moved into archive/ locally, never committed.
# The repo holds only the judge-facing artifacts; orchestrator scaffolding
# (PRD, BRAINSTORM, CICD_SPEC, epics, stories, ux-spec, etc., plus the
# context/ and research/ corpora that informed them) lives in archive/ on
# disk for the author's records, out of git.
archive/
AGENTS.md
.private/
*.original.md
.vercel

.kite-passport/
